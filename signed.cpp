#include <iostream>
using namespace std;
int main()
{
    signed int x = -5; // Declare a signed integer variable named 'x' and initialize it with the value -5. Signed integers can represent both positive and negative values, so 'x' will hold the value -5 without any issues.
    cout << x << "\n";
    return 0;
}